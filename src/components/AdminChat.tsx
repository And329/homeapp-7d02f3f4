
import React from 'react';
import AdminChat from './messaging/AdminChat';

// This is a wrapper component to maintain compatibility
const AdminChatWrapper: React.FC = () => {
  return <AdminChat />;
};

export default AdminChatWrapper;
